# zygisk_gadget 使用教程

自定义 ELF Linker 加载 stealth frida-gadget —— 完整使用指南。

---

## 一、准备

| 项 | 要求 |
|---|---|
| 手机 | **Android 10+**，已 root：KernelSU / Magisk + Zygisk（或 Zygisk Next） |
| 电脑 | Windows + adb（USB 调试已开启） |
| 成品 | 从 Release 下载 `zygisk_gadget_delivery.zip`（含全部部署文件） |

交付包内容：

```
deploy.ps1               一键部署脚本（自包含）
libgadget.so             frida-gadget 17.9.1 自编译版（桥接补丁+线程名+混淆）
libxiaojia.so            Zygisk 模块（自定义 ELF linker + MAP_JIT）
config.json              模块配置（含目标包名，部署前请自行修改）
libhhh.config.so         gadget 配置（监听端口 14725）
zygisk_gadget-v1.0.0.zip 模块刷机包（管理器安装备用）
说明.txt
```

## 二、部署（1 条命令）

```powershell
# 解压交付包 → 手机连电脑（adb devices 能看到）→ 运行：
powershell -ExecutionPolicy Bypass -File deploy.ps1
```

脚本自动完成：
1. 检测设备
2. push 4 个文件到 `/data/local/tmp/`（先 push 再 su cp，规避 KernelSU namespace 隔离）
3. su 安装到模块目录（`cp -f` 规避权限问题）：
   - `libxiaojia.so` → `/data/adb/modules/zygisk_gadget/zygisk/arm64-v8a.so`
   - `libgadget.so` → `/data/adb/modules/zygisk_gadget/libgadget.so`
   - `libhhh.config.so` → `/data/adb/modules/zygisk_gadget/`
   - `config.json` → `/data/adb/zygisk_gadget/config.json`
4. **自动重启手机**（Zygisk 模块替换必须重启，zygote 缓存旧 SO）

> 备选：用 KernelSU/Magisk 管理器安装 `zygisk_gadget-v1.0.0.zip`，再手动 push 其余文件，同样重启。

## 三、启动与验证

重启完成后**打开目标游戏**，另开终端：

```powershell
adb logcat -s ZygiskGadget CustomLinker Frida
```

成功标志（完整日志）：

```
D [ZygiskGadget]: Frida-gadget injection thread start for <包名>,
    gadget name: libhhh.so, usleep: 0, use_custom_linker: 1
D CustomLinker: Set FRIDA_GADGET_RANGE=... for detect_location
D CustomLinker: Relocation complete for libhhh.so      ← 自定义 linker 加载成功
D CustomLinker: Calling 8 init_array functions
I Frida   : Listening on 0.0.0.0 TCP port 14725        ← 关键：监听就绪
D [ZygiskGadget]: Frida-gadget loaded via custom linker
D [ZygiskGadget]: Deleted gadget file: .../libhhh.so   ← 磁盘痕迹已清理
```

同时确认：游戏不闪退、`/data/tombstones` 无新增文件。

## 四、连接 Frida

```powershell
adb forward tcp:14725 tcp:14725
frida -H 127.0.0.1:14725 -n Gadget
frida -H 127.0.0.1:14725 -n Gadget -l your_script.js
```

## 五、修改配置

模块配置在手机 `/data/adb/zygisk_gadget/config.json`（**不是 modules/ 下**，扁平格式，包名做顶层 key，改后需重启）：

```json
{
  "com.netease.TomJerry": {
    "inject": true,
    "use_custom_linker": true,
    "delay_us": 0,
    "pin_module": false,
    "gadget_name": "libhhh.so",
    "gadget_config": "libhhh.config.so"
  }
}
```

| 字段 | 默认 | 说明 |
|---|---|---|
| `inject` | false | 是否注入该包 |
| `use_custom_linker` | false | **必须 true**（自定义 linker 加载） |
| `delay_us` | 0 | 注入延迟微秒（>0 时自动 pin 模块防卸载竞态） |
| `pin_module` | false | true=模块留在内存（一般 false 更无痕） |
| `gadget_name` | libhhh.so | 拷贝到 app 目录的伪装文件名 |
| `gadget_config` | libhhh.config.so | 配置文件在模块目录中的名字 |

换目标 app：把包名替换成你的目标（支持多包并列写多个）。

**改端口**：编辑 `libhhh.config.so`（JSON：`port` 字段）→ 覆盖到 `/data/adb/modules/zygisk_gadget/` → 重启游戏进程。

## 六、反检测核查（游戏运行中，su 下）

```powershell
adb shell "su -c 'cat /proc/$(pidof <包名>)/maps | grep -E \"jit|anon|frida\"'"
# 期望：只见 /memfd:jit-cache 风格段（与 ART 一致），无 frida 路径、无 [anon] 代码段

adb shell "su -c 'ls /proc/$(pidof <包名>)/task/*/comm | xargs cat'"
# 期望：线程名无 frida/gadget 关键字（art.worker）

adb shell "su -c 'ls -la /proc/$(pidof <包名>)/fd | grep -c memfd'"
# 期望：无 memfd:jit-cache fd（已关闭）
```

## 七、常见问题

| 现象 | 原因与处理 |
|---|---|
| 只有 injection thread start，无 CustomLinker | config.json 没读到：检查路径 `/data/adb/zygisk_gadget/config.json`、包名大小写、JSON 格式；改完必须重启 |
| Relocation complete 但无 Listening | 用了非自编译 gadget → 换交付包 `libgadget.so` |
| 端口是 27042 | libhhh.config.so 未部署或游戏进程未重启 |
| 游戏闪退 | `adb logcat -b crash` 查崩溃；确认 config 正确 |
| 日志出现 `converting to anonymous` | MAP_JIT 未生效（Android < 10），功能不受影响 |
| 换模块/配置不生效 | 必须 `adb reboot`（zygote 缓存旧模块），force-stop 无效 |
| `KPM: PING -> absent` | 正常——KPM 内核层未安装（可选增强，见下文） |

## 八、KPM 内核层（可选增强）

反检测体系中的内核层（inode 伪装 / 代码段 shadow）需要 KernelPatch 环境：

1. 前提：手机内核启用 KernelPatch（APatch / SukiSU）
2. 验证链路：`kpmctl <superkey> load 模块.kpm` + `dmesg | grep kpm`
3. 可参考：`kpm-spore`（hidemap/trace_maps 隐藏 maps 痕迹）、`KernelPatch` 官方示例
4. 加载成功后模块日志变为 `KPM: PING -> 0x4b504d48 (present)`

> 文档中的 `ReportJitCacheSpoof` / `text_shadow` 为私有内核模块，无公开源码，需自研。

## 九、从源码重新构建（可选）

| 目的 | 操作 |
|---|---|
| 改模块代码 | `scripts\build.ps1`（Windows + NDK，产物自动同步到 release/） |
| 重新编译 gadget | WSL 内 `bash scripts/build_wsl.sh`（需 frida 17.9.1 源码 + NDK r29） |
| 线程名/字符串混淆 | 构建脚本内置（gadget-glue 补丁 + obfuscate 脚本） |
| 打包交付 | 压缩 `release/` 目录即可 |

## 十、架构速览

```
libxiaojia.so (Zygisk 模块)
  └─ injection()
       └─ mylinker_load_library(gadget_path)
            ├─ ElfReader::Open/Read            — 读 ELF
            ├─ MemoryManager::ReserveAddressSpace — memfd:jit-cache + MAP_JIT
            ├─ MemoryManager::LoadSegments     — 拷贝段
            ├─ MemoryManager::ProtectSegments  — 权限设置（execmod 自动回退）
            ├─ SoinfoManager::PrelinkImage     — 解析 .dynamic
            └─ Relocator::LinkImage            — 重定位 + init/init_array

关键桥接（环境变量，即用即清）：
  FRIDA_GADGET_RANGE   → base,size（修复 detect_location 断言）
  FRIDA_GADGET_CONFIG  → 配置内容（不走文件，端口生效）
```

详细技术原理见 `README.md`（五大问题根因与修法、代码审查记录、反检测矩阵）。
